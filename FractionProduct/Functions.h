#ifndef FUNTIONS_H
#define FUNTIONS_H

#include "Fraction.h"

auto FractionProduct(const Fraction& f1, const Fraction& f2) -> Fraction;
auto PrintFraction(const Fraction& f) -> void;
auto getFraction() -> Fraction;

#endif